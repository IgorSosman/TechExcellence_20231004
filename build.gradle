plugins {
    id 'java'
}




tasks.register('createUseCaseFolderStructure') {
    doFirst {
        var relPath = project.projectDir.path + '/bookstore/src/main/java/ch/sosman/bookstore/application/' +
                useCaseName
        new File((relPath + '/application/model')).mkdirs()
    }
}